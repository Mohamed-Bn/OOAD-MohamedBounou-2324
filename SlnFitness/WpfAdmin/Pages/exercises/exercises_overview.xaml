<Page x:Class="WpfAdmin.Pages.exercises.exercises_overview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfAdmin.Pages.exercises"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="700"
      Title="exercises_overview">

    <ScrollViewer>
        <Grid x:Name="rectangleGrid" Background="#FFFBFBFB" ScrollViewer.HorizontalScrollBarVisibility="Auto" ShowGridLines="True" UseLayoutRounding="True" ScrollViewer.CanContentScroll="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel x:Name="exercise_name_panel" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,10,10">
                <!-- Buttons for exercise names will be added dynamically -->
            </StackPanel>

            <Grid Grid.Row="1" x:Name="exercisesGrid">
                <!-- Dynamic exercise content goes here -->
            </Grid>
            <Frame Grid.Row="1" x:Name="ChildFrame"/>
        </Grid>
    </ScrollViewer>

</Page>
